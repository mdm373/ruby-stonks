# ruby-stonks-api
> The API for tradin' stonks

## Local Build & Serve
* Configure
  * copy `.env.shadow` to `.env` and modify as need
* Run
  * `gem install rails`
  * `bundle install`
  * `rails server`

## Run DB Migrations
* `rails db:migrate`